export function toSortedObjectArray(set) {
    if (!set)
        return [];
    return toSortedArray(set).map(([k, v]) => ({ [k]: v }));
}
export function toSortedArray(set) {
    if (!set)
        return [];
    return Object.entries(set).sort(([a], [b]) => a.localeCompare(b));
}
export function toSortedObject(set) {
    if (!set)
        return set;
    return Object.fromEntries(toSortedArray(set));
}
//# sourceMappingURL=sorted-object.js.map
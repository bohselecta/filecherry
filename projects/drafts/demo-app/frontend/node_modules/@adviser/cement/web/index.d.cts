import { W as WrapperSysAbstractionParams } from '../base-sys-abstraction-DAZ8tx5F.cjs';
import { F as FileService, N as NamedWritableStream, S as SysAbstraction } from '../sys-abstraction-BTs3Vzy4.cjs';
import '../get-params-result-4xHM5s4_.cjs';

declare class WebFileService implements FileService {
    get baseDir(): string;
    create(fname: string): Promise<NamedWritableStream>;
    readFileString(fname: string): Promise<string>;
    writeFileString(fname: string, content: string): Promise<void>;
    abs(fname: string): string;
    join(...paths: string[]): string;
    relative(from: string, to?: string): string;
    dirname(fname: string): string;
    basename(fname: string): string;
    nodeImport(fname: string): string;
    isAbsolute(fname: string): boolean;
}
declare function WebSysAbstraction(param?: WrapperSysAbstractionParams): SysAbstraction;

export { WebFileService, WebSysAbstraction };
